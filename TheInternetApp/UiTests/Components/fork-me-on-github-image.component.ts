import { Selector, t } from 'testcafe';

class ForkMeOnGithubImageComponent {
        public forkMeOnGithubImage = Selector('body > div:nth-child(2) > a > img');

}

export default ForkMeOnGithubImageComponent;